apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-edgware
  namespace: edgware
spec:
  storageClassName: database
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi